# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: smoke.spec.ts >> Home page renders header with h1 "DropMod"
- Location: e2e/smoke.spec.ts:37:1

# Error details

```
Error: expect(locator).toHaveText(expected) failed

Locator: locator('h1')
Expected pattern: /DropMod/
Received string:  "Minecraft を彩る、Modpacksの玄関口。"
Timeout: 5000ms

Call log:
  - Expect "toHaveText" with timeout 5000ms
  - waiting for locator('h1')
    11 × locator resolved to <h1 id="hero-title" class="text-3xl sm:text-5xl lg:text-6xl font-extrabold tracking-tight leading-tight">…</h1>
       - unexpected value "Minecraft を彩る、Modsの玄関口。"
    3 × locator resolved to <h1 id="hero-title" class="text-3xl sm:text-5xl lg:text-6xl font-extrabold tracking-tight leading-tight">…</h1>
      - unexpected value "Minecraft を彩る、Modpacksの玄関口。"

```

```yaml
- heading "Minecraft を彩る、 Modpacks の玄関口。" [level=1]
```

# Test source

```ts
  1  | /**
  2  |  * スモークテスト: 全主要ページが 200 で返ること
  3  |  */
  4  | import { test, expect } from '@playwright/test';
  5  | 
  6  | // ルーティング再設計: 検索一覧は /discover/<複数形>
  7  | const PAGES = [
  8  |   '/',
  9  |   '/mods',
  10 |   '/discover/mods',
  11 |   '/discover/modpacks',
  12 |   '/discover/resourcepacks',
  13 |   '/discover/shaders',
  14 |   '/profile',
  15 |   '/settings',
  16 |   '/modpack',
  17 |   '/resourcepack',
  18 |   '/shader',
  19 |   '/api/health',
  20 |   '/sitemap.xml',
  21 |   '/robots.txt',
  22 |   '/manifest.webmanifest'
  23 | ];
  24 | 
  25 | for (const path of PAGES) {
  26 |   test(`GET ${path} returns 200`, async ({ request }) => {
  27 |     const res = await request.get(path);
  28 |     expect(res.status()).toBe(200);
  29 |   });
  30 | }
  31 | 
  32 | test('GET /nonexistent returns 404', async ({ request }) => {
  33 |   const res = await request.get('/nonexistent');
  34 |   expect(res.status()).toBe(404);
  35 | });
  36 | 
  37 | test('Home page renders header with h1 "DropMod"', async ({ page }) => {
  38 |   await page.goto('/');
> 39 |   await expect(page.locator('h1')).toHaveText(/DropMod/);
     |                                    ^ Error: expect(locator).toHaveText(expected) failed
  40 |   // h1 は 1 個だけ (C6-1 継続確認)
  41 |   await expect(page.locator('h1')).toHaveCount(1);
  42 | });
  43 | 
```