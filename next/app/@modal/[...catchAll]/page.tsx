// -----------------------------------------------------------------------------
// @modal Parallel Slot のキャッチオールルート
//
// これが無いと、モーダルを開いた後にブラウザ更新 or 別ページに hard navigate
// した際に「slot が閉じたつもりでも古いモーダルが残る」不具合が起きる。
// default.tsx とセットで必ず用意する。
// -----------------------------------------------------------------------------
export default function ModalCatchAll() {
  return null;
}
